---
export type IconProps = {
	'aria-hidden'?: boolean;
	size?: number | string;
	'stroke-width'?: number | string;
};

export type RawIcon = (props: IconProps) => unknown;

export type Props = {
	icon: RawIcon;
	size?: string | number;
};

const { icon: RawIcon, size = '1em' } = Astro.props;
---

<RawIcon aria-hidden size={size} stroke-width="2" />

[package]
name = "vertx"
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false

[lints]
workspace = true

[features]
esp32 = [
    "dep:esp-backtrace",
    "dep:esp-hal",
    "dep:esp-hal-smartled",
    "dep:esp-storage",
    "dep:esp-wifi",
]
esp32s3 = [
    "esp32",
    "esp-backtrace/esp32s3",
    "esp-hal/esp32s3",
    "esp-hal-smartled/esp32s3",
    "esp-storage/esp32s3",
    "esp-wifi/esp32s3",
]
simulator = [
    "embassy-executor/arch-std",
    "embassy-executor/executor-thread",
    "embassy-net/std",
    "embassy-time/std",
    "dep:embassy-net-tuntap",
    "dep:env_logger",
    "dep:rand",
]

[dependencies]
bytemuck = { version = "=1.16.0", features = ["min_const_generics"] }
embassy-executor = { version = "0.5.0", features = ["nightly"] }
embassy-futures = "0.1.1"
embassy-net = { workspace = true, features = ["dhcpv4", "log", "tcp", "dhcpv4-hostname"] }
embassy-sync = { workspace = true }
embassy-time = { version = "0.3.0", features = ["generic-queue-8"] }
esp-alloc = { version = "0.3.0" }
esp-println = { version = "0.9.1", features = ["esp32s3", "log"] }
heapless = { workspace = true }
log = { version = "0.4.21" }
picoserve = { workspace = true, features = ["embassy"] }
portable-atomic = { version = "1.6.0", default-features = false }
postcard = { workspace = true }
serde = { workspace = true, features = ["derive"] }
smart-leds = "0.4.0"
static_cell = { version = "2.1.0", features = ["nightly"] }
vertx-config = { workspace = true, features = ["heapless"] }

# esp32
esp-backtrace = { version = "=0.11.1", features = ["colors", "panic-handler", "exception-handler", "println"], optional = true }
esp-hal = { version = "0.17.0", features = ["async", "embassy", "embassy-executor-thread", "embassy-time-timg0"], optional = true }
esp-hal-smartled = { version = "0.10.0", optional = true }
esp-storage = { version = "0.3.0", default-features = false, features = ["low-level", "storage", "critical-section"], optional = true }
esp-wifi = { version = "=0.5.1", features = ["wifi", "embassy-net"], optional = true }

# simulator
embassy-net-tuntap = { version = "=0.1.0", optional = true }
env_logger = { version = "=0.11.3", optional = true }
rand = { version = "=0.8.5", optional = true }

[build-dependencies]
cargo_toml = "=0.20.2"
prettyplease = "=0.2.20"
quote = "1.0.36"
serde = { workspace = true, features = ["derive"] }
serde_json = "=1.0.117"
syn = { workspace = true, features = ["parsing"] }

[[bin]]
name = "simulator"
required-features = ["simulator"]
